{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --prefer-dist && php artisan key:generate && php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force --no-interaction && php-fpm -y /usr/local/etc/php-fpm.conf"
  }
}
